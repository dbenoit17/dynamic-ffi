#lang racket/base

(require ffi/unsafe)

(provide (all-defined-out))

(define ~a ~a)

(define ~a-headers ~a)

(define ((warn-undefined-symbol sym))
  (eprintf "warning: ~a does not contain symbol ~~a\n" sym))

~a 
